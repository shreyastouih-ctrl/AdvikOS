# AdvikOS - Ultra-Low RAM Compression Configuration
# Dynamically turns 1GB physical RAM into ~2.2GB usable virtual memory
[zram0]
zram-size = ram * 1.5
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
